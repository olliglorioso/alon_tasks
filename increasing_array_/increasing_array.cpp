#include <string>
#include <iostream>
#include <sstream>
#include <vector>
#include <utility>
#include <iterator>
using namespace std;

int main() {
    // int size_of_array; 
    // cin >> size_of_array;
    // read a line of numbers and turn them into an array
    int n;
    cin >> n;
    for (int i = 1; i <= n; i++) {
        int x;
        cin >> x;
    }

    // cout << 5;
}